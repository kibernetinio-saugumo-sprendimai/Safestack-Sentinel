# Release checklist

Create and push a version tag: `git tag v0.2.0 && git push origin v0.2.0`.
The release workflow packages source and profiles and publishes SHA-256 checksums.
Keep Ed25519 private keys outside GitHub.
